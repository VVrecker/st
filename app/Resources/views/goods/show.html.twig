{% extends 'base.html.twig' %}

{% block body %}
    <h1>Goods</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ good.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ good.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('goods_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('goods_edit', { 'id': good.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
